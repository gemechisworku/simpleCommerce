feat: Phase 1 - Foundation & Core Infrastructure Implementation

Implement complete foundation layer for simpleCommerce platform including
database models, migrations, core utilities, and API infrastructure.

## Database Models & Schema
- Create all 11 database models (Users, Products, Orders, Payments, etc.)
- Implement multi-identity user support (phone, email, Telegram)
- Add product catalog models with variants and images
- Create order management models with status tracking
- Implement payment workflow models with review system
- Add notification and authentication models (OTP, refresh tokens)

## Database Migrations
- Create initial Alembic migration with all tables
- Add enum types for user roles, order statuses, payment statuses
- Implement database indexes for performance optimization
- Add foreign key constraints and check constraints
- Create database triggers for automatic updated_at timestamps
- Handle enum type creation with duplicate-safe logic

## Core Configuration
- Complete settings management with environment variables
- Configure database connection (PostgreSQL)
- Set up JWT authentication settings
- Configure MinIO/S3 file storage
- Add OTP, file upload, and pagination settings

## Database & Session Management
- Implement SQLAlchemy engine and session factory
- Create get_db() dependency for FastAPI dependency injection
- Add connection pooling and health checks

## Security Utilities
- Implement JWT access token generation (15 min expiry)
- Implement JWT refresh token generation (7 days expiry)
- Add token verification and user ID extraction functions

## File Storage
- Set up MinIO client for S3-compatible storage
- Implement file upload functionality
- Add bucket creation and file deletion utilities

## Utility Functions
- OTP code generation
- Slug generation from text
- Order number generation
- Phone and email validation
- Delivery date calculation helpers

## Error Handling
- Create custom exception classes (ValidationError, AuthenticationError, etc.)
- Implement global exception handler in FastAPI
- Add proper HTTP status codes and error response format

## Logging
- Configure structured logging
- Set up log levels based on environment
- Add SQL query logging for debug mode

## API Structure
- Set up API router with /api/v1 prefix
- Create base response schemas (ResponseModel, PaginatedResponse)
- Implement authentication dependencies (get_current_user, require_role)
- Add health check endpoints (/health, /health/detailed)
- Configure CORS middleware

## Testing & Verification
- All models compile and import successfully
- Database migration runs successfully
- Health check endpoints verified
- All 15 tables created in database

## Files Created
- 11 model files in app/models/
- Core utilities in app/core/ (config, database, security, storage, exceptions, logging)
- Helper functions in app/utils/
- API structure in app/api/
- Initial migration in app/migrations/versions/
- Base schemas in app/schemas/

This foundation enables Phase 2 (Authentication & User Management) development.

